const mediaList = (state) => state.mediaSearchResult;
const watchList = (state) => state.watchList;
const isLoader = (state) => state.isLoader;

export default {
  mediaList,
  watchList,
  isLoader,
};
